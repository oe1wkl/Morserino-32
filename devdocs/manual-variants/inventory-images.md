# Inventory: images

Generated by `make_inventories.py`. Do not edit by hand.

`images/` holds 5 files; 4 are referenced from the Markdown and one (`M32_logo.png`) from the title pages.

| Image | Proposed class | EN line | DE line | Alt text | Notes |
|---|---|---|---|---|---|
| `images/m32_1st_edition.jpg` | **classic** | 92 | 100 | *(none)* | Photo of the 1st edition, under its own section (already tagged). |
| `images/m32_2nd_edition.jpg` | **classic** | 66 | 74 | *(none)* | Photo of the 2nd edition, under its own section (already tagged). |
| `images/m32pocket.jpg` | **pocket** | 47 | 55 | *(none)* | Photo of the M32 Pocket, under the Pocket connectors section (already tagged). |
| `images/status_line.png` | **classic** | 407 | 431 | *(none)* | Status line screenshot. The manual's own note says it shows the older Morserinos and that the Pocket 'is similar' -- so it is an OLED screenshot standing in for both. Needs a Pocket counterpart before it can be tagged. |
| `images/M32_logo.png` | **common** | - | - | - | Used by title.html / title_de.html, not from the Markdown. |

## What this means for session 2

- Three of the four referenced images are already inside a variant div,
  so per-variant image paths are not strictly needed for them -- the
  surrounding tag already removes them.
- `status_line.png` is the one real case: it is an OLED screenshot shown
  to every reader, with a note apologising for it. A Pocket screenshot
  plus a per-variant path (or two tagged image references) would let the
  note go away.
- **Every image reference in the manual has empty alt text.** For the
  accessibility build that is a defect in its own right: a screen reader
  announces nothing at all. Alt text is new content, so it is out of
  scope here, but it belongs on the session-2 list.
