# Inventory: images

Generated by `make_inventories.py`. Do not edit by hand.

`images/` holds 5 files; 4 are referenced from the Markdown and one (`M32_logo.png`) from the title pages.

| Image | Proposed class | EN line | DE line | Alt text | Notes |
|---|---|---|---|---|---|
| `images/m32_1st_edition.jpg` | **classic** | 110 | 111 | The Morserino-32 1st edition: the circuit board in a clear acrylic case, seen from above. The loudspeaker is the round grille on the right, the display the small panel on the left. Numbered labels 1 to 14 mark the connectors and controls listed in the table below. | Photo of the 1st edition, under its own section (already tagged). |
| `images/m32_2nd_edition.jpg` | **classic** | 84 | 85 | The Morserino-32 2nd edition: the circuit board in a clear acrylic case, seen from above. The loudspeaker is the round grille on the right, the display the small panel on the left. Numbered labels 1 to 15 mark the connectors and controls listed in the table below. | Photo of the 2nd edition, under its own section (already tagged). |
| `images/m32pocket.jpg` | **pocket** | 42 | 43 | The M32 Pocket: a black 3D-printed case, seen from above at an angle. Its top face is mostly taken up by the display window; the rotary knob sits at the far end. Numbered labels 1 to 8 mark the connectors and controls listed in the table below. | Photo of the M32 Pocket, under the Pocket connectors section (already tagged). |
| `images/status_line.png` | **classic** | 425 | 445 | Close-up of the status line across the top of the display: the keyer-mode letter B on the left, the speed 20 WpM in the middle, and on the right the volume bar followed by the wireless-transmission symbol. | Status line screenshot. The manual's own note says it shows the older Morserinos and that the Pocket 'is similar' -- so it is an OLED screenshot standing in for both. Needs a Pocket counterpart before it can be tagged. |
| `images/M32_logo.png` | **common** | - | - | - | Used by title.html / title_de.html, not from the Markdown. |

## Where this stands

- **Alt text: done.** Each image says what it shows and notes that the numbers match the table below; pandoc now declares schema:accessibilityFeature alternativeText in the EPUB.
- Three of the four referenced images are already inside a variant div,
  so per-variant image paths are not strictly needed for them -- the
  surrounding tag already removes them.
- The M32 Pocket photo is followed by a `{.pocket-a11y}` block describing
  the device by touch, since that is the only photo a reader of the
  accessibility edition meets. It is drafted and marked in the source with
  a TODO: the touch-paddle and FN-button positions need checking against a
  real device.
- `status_line.png` is the one still-open case: an OLED screenshot shown
  to Pocket readers too, with a note apologising for it. A Pocket
  screenshot plus a per-variant path (or two tagged image references)
  would let the note go away.
